<UserControl x:Class="UI.Desktop.GridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="0,-1,0,1">
        <ScrollViewer HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid>
                <Rectangle HorizontalAlignment="Stretch" Stroke="White" Fill="White" VerticalAlignment="Top" Height="5"/>
                <WrapPanel x:Name="stackPanel" Margin="0,5,0,0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
